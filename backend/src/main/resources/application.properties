# OIDC — token issued by Keycloak
quarkus.oidc.auth-server-url=http://localhost:8180/realms/quantum-health
quarkus.oidc.client-id=quantum-health-backend
quarkus.oidc.application-type=service
# Keycloak Admin Client — used by KeycloakDoctorAdapter
quarkus.keycloak.admin-client.server-url=http://localhost:8180
quarkus.keycloak.admin-client.realm=master
quarkus.keycloak.admin-client.client-id=admin-cli
quarkus.keycloak.admin-client.username=admin
quarkus.keycloak.admin-client.password=admin
quarkus.keycloak.admin-client.grant-type=PASSWORD
quantum-health.keycloak.realm=quantum-health
# Liquibase
quarkus.liquibase.migrate-at-start=true
quarkus.liquibase.change-log=db/changelog/db.changelog-master.yaml
# Hibernate — validate schema against entities at startup
quarkus.hibernate-orm.schema-management.strategy=validate
